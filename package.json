{
  "name": "abyssal-nebula",
  "version": "1.0.0",
  "description": "Nebula - A tabbed proxy browser with WebSocket streaming powered by Puppeteer",
  "main": "server.js",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": ["proxy", "browser", "puppeteer", "websocket"],
  "author": "Proxy-finder",
  "license": "MIT",
  "dependencies": {
    "express": "^5.2.1",
    "puppeteer-core": "^24.37.3",
    "ws": "^8.19.0"
  }
}